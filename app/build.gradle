plugins {
    alias(libs.plugins.android.application)
}

android {
    aaptOptions {
        additionalParameters "--package-id", "0x8f"
    }
    signingConfigs {
        mig_tyt {
            storeFile file('Key_Tyveits.jks')
            storePassword 'l114514ht'
            keyAlias 'mig'
            keyPassword 'l114514ht'
        }
    }
    namespace 'com.android.setup'
    compileSdk {
        version = release(36) {
            minorApiLevel = 1
        }
    }

    defaultConfig {
        applicationId "com.android.setup"
        minSdk 24
        //noinspection ExpiredTargetSdkVersion
        targetSdk 36
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            signingConfig = signingConfigs.getByName("mig_tyt")
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_11
        targetCompatibility JavaVersion.VERSION_11
    }
}

dependencies {
    implementation libs.appcompat
    implementation libs.material
    implementation libs.activity
    implementation libs.constraintlayout
    implementation libs.fragment
    testImplementation libs.junit
    androidTestImplementation libs.ext.junit
    androidTestImplementation libs.espresso.core
    implementation 'androidx.preference:preference:1.2.1'
    implementation 'io.github.liukai2530533:round_circle_imageview:1.0.1'
}